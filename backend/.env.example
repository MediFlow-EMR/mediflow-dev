# MediFlow EMR - Backend Environment Variables
# 이 파일을 복사해서 .env 파일을 만들고 실제 값을 입력하세요
# cp .env.example .env

# ===========================================
# 데이터베이스 설정 (MariaDB)
# ===========================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=mediflowdb
DB_USERNAME=root
DB_PASSWORD=your_database_password_here

# ===========================================
# JWT 설정
# ===========================================
# JWT Secret Key (256비트 이상, Base64 인코딩 권장)
# 생성 방법: openssl rand -base64 32
JWT_SECRET=your-base64-encoded-secret-key-here

# Access Token 만료 시간 (밀리초)
# 900000 = 15분
JWT_ACCESS_TOKEN_EXPIRATION=900000

# Refresh Token 만료 시간 (밀리초)
# 604800000 = 7일
JWT_REFRESH_TOKEN_EXPIRATION=604800000

# ===========================================
# OAuth2 - Google
# ===========================================
# Google Cloud Console에서 발급
# https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ===========================================
# OAuth2 - Kakao
# ===========================================
# Kakao Developers에서 발급
# https://developers.kakao.com/
KAKAO_CLIENT_ID=your-kakao-client-id
KAKAO_CLIENT_SECRET=your-kakao-client-secret

# ===========================================
# 이메일 설정 (Naver SMTP)
# ===========================================
# Naver 메일 설정 방법:
# 1. 네이버 메일 로그인
# 2. 환경설정 → POP3/IMAP 설정
# 3. IMAP/SMTP 사용 '사용함' 체크
MAIL_USERNAME=your-email@naver.com
MAIL_PASSWORD=your-naver-app-password

# ===========================================
# 서버 설정
# ===========================================
SERVER_PORT=9005

# ===========================================
# 식약처 API 설정
# ===========================================
# 공공데이터포털에서 발급
# https://www.data.go.kr/
# "의약품 정보" 검색 → 활용신청
DRUG_API_SERVICE_KEY=your-drug-api-service-key

# ===========================================
# Google Gemini AI 설정
# ===========================================
# Google AI Studio에서 발급
# https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key
